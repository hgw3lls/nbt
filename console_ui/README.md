# Neural Bending Console UI

```bash
npm install
npm run dev
```

Backend should be running via:

```bash
nbt console serve --host 127.0.0.1 --port 8000
```

See `../docs/CONSOLE_UI.md` for full details.
